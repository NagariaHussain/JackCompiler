// My good comment
class BST {
    field int x, y, z, w;

    function char main() {
    }

    method void halwa(int x, char y) {
        if (x) {
            let a = y;
        } else {
            let z = x;
        }
    }

    constructor BST new(char a, char b, boolean c) {
        var int a, b, c;

        if (x) {
            let x = c;
        }
    }
}