// My good comment
class BST {
    field int x, y, z, w;

    function char main() {
        do sayHello();
        do Memory.alloc();
    }

    method void halwa(int x, char y) {
        if (x) {
            let a = y;
        } else {
            let z = x;
        }
    }

    constructor BST new(char a, char b, boolean c) {
        var int a, b, c;

        while(x) {
            let x = y;
            if (x) {
                let z = x;
            }
        }
    }
}